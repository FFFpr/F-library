---
description: 禁止阅读其他 agent 的上下文，除非用户明确要求
alwaysApply: true
---

# 禁止阅读其他 agent 的上下文

未经用户明确要求，不得读取、拉取或根据其他 agent 的对话记录来推断当前任务。

禁止包括：列出或打开其他 cloud agent / 会话；读取其 transcript、events、提示词或摘要；把其他会话里的用户原文、论断、附件当成当前任务的材料。

当前消息没有论断、附件或上下文时，直接问用户要评什么、对照哪本书。不要用其他会话补全。

仅当用户写明要看某个 agent、某次对话或「去读那个会话」时，才可以读取指定对象。任务名称相似、同一仓库、同一环境、缺少材料，都不构成允许。
