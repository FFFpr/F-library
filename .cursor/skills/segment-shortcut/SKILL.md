---
name: segment-shortcut
description: 按主旨、最小标签集、形容词分档和稀疏关联，把原书段落写成有序列的段落总结。仅在用户调用 /segment-shortcut、点名 segment-shortcut，或明确要求按本 skill 写段落总结序列时使用。
disable-model-invocation: true
---

# Segment Shortcut

把已经读完的章节正文写成段落总结序列。

档由形容词定义。标签只从本文件目录取用。本段的档，按该段所用标签的含义对照档的形容词判定。

## 档

- 第一档：不可取缔的，必需的，定义的
- 第二档：可替换的，旁证的
- 第三档：依附于某段的，过渡的，主旨无关的

## 标签目录

每项含：含义、与其他标签的关系。

| 标签 | 含义 | 关系 |
|---|---|---|
| 陈述 | 直接说出本标题某条主旨；对该条是定义的、必需的、不可取缔的 | 被限定、例外划界 |
| 必要依据 | 给出主旨所依赖的机制或定义；删掉则主旨不能成立，是必需的、不可取缔的 | 与可替换例证互替：先出现的机制用本标签 |
| 限定 | 缩小主旨适用范围；对该范围是定义的、必需的 | 划界→陈述 |
| 例外 | 标明通例在此处不适用；对主旨边界是定义的、不可取缔的 | 划界→陈述 |
| 可替换例证 | 用作品、引文、场景支撑主旨；同标题还有同类例子，是可替换的、旁证的 | 与必要依据互替：机制已有之后的作品、引文、场景用本标签 |
| 次要观点 | 不是本标题主旨，但能旁证主旨；是可替换的、旁证的 | 旁证→陈述 |
| 续 | 跨页未完的引文、同一套曲辞的后续阕、接在「……」后的续抄；依附于起始段 | 依附→起始段 |
| 过渡/场景 | 转场、纯景物，或与主旨无关；是过渡的、依附于上下文的 | — |

## 顺序

先通读全文，确定两件事：

1. **主旨**：当前原书小节标题下 1～3 条，每条一句话，依据该标题原文。若与章首核心论点不完全重合，在序列开头标明差异。
2. **最小标签集**：从目录取出一组标签，覆盖本标题段落之间实际出现的关系。每个段的标签必须来自这组。

再按原文物理段顺序写成有序列 `1.` `2.` `3.`，条数等于物理段数。对每一段：从最小标签集分配一个标签，再按该标签含义对照档的形容词定档。续段标「续 n」（n 为起始段号）。

## 关联与篇幅

每段至多 3 条出边，关系仅限：`证明→` `被证明←` `实例←` `例外于` `限定→` `续`。写在条目末尾括号内，如 `(证明→6,12)` `(续 8)`。

枢纽段：被至少两条指向，或第一档且指出至少两条。

- 第一档或枢纽段，写三块：功能一句；依据（并列的因分条）；在本标题主旨中的位置一句。
- 第二档：不超过三句。
- 第三档：一句；续段为「续 n：……」。

## 格式

```
3. 【第一档·必要依据】童心说把市民写实转成个性解放的创作标准。
   依据：……
   位置：本标题主旨「上层浪漫」的机制段。
   (证明→6,12,14)(被证明←2)
```

第一行含段号、档、标签、本段功能。括号只放关联。
