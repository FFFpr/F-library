---
name: explain
description: 按自明指称和前因后果解释一个具体概念、机制或片段。仅在用户调用 /explain 时使用。
disable-model-invocation: true
---

# Explain

解释用户提出的问题。同一条消息里没有要求实现，就不要开始改代码。

## 约束

- 不要使用读者必须离开当前回复才能理解的指针：未展开的原则编号、未点名的缩写、找不到先行词的代词。
- 每个指称在当句或紧邻句给出名称和足够上下文。
- 缩写在首次出现时写全称；之后可以使用缩写。
- 代词必须在当前回复里有明确先行词。不要使用悬挂的「它 / 这 / 上述」。
- 引用外部材料时，先用一句话说清它主张什么。链接只作核验，不承担语义。
- 不要只写结果。涉及机制时，在同一次说明里写清条件、动作、结果。
- 不要用比喻代替机制。需要直觉时，先写机制，再标明类比在何处断开。
- 不要把「有逻辑 / 严谨 / 讲清楚」当作可执行约束。
- 数学公式：网页端用 Unicode；否则用 KaTeX。用户若声明当前入口，以该声明为准。
  KaTeX 细则：
  1. 独立公式用 `$$ ... $$` 单独成段，不与正文挤在同一行。
  2. 行内公式用 `\(` 与 `\)` 包裹；禁止单个 `$ ... $`。
  3. 公式不要放进 markdown 代码块，也不要再套一层方括号当容器。
  4. 定义域、求和范围等元信息不写入公式同一行；另起一行，或写在公式下的一句说明里。
  5. 已按第 1–4 条发送，显示端仍展示 TeX 源码时：判定当前客户端未能渲染（render）KaTeX，改用 Unicode 书写同一公式。
